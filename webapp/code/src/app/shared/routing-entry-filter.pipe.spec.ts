import { RoutingEntryFilterPipe } from './routing-entry-filter.pipe';

describe('RoutingEntryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoutingEntryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
